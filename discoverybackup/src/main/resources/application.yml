server:
  port: 8762

eureka:
  instance:
    hostname: myservername
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://thirdservername:8763/eureka/
fetch-registry: false